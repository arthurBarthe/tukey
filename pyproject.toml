[tool.poetry]
name = "tukey"
version = "0.1.0"
description = "Probabilistic loss functions for neural networks in Pytorch"
authors = ["arthur <ahw795@qmul.ac.uk>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.8"
scipy = "^1.9.3"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
